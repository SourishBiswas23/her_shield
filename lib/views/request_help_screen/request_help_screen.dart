import 'package:flutter/material.dart';

class RequestHelpScreen extends StatelessWidget {
  const RequestHelpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('This is the request help screen'),
      ),
    );
  }
}
